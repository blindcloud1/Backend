services:
  mongo:
    image: mongo:7
    container_name: blindscloud-mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USER:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD:-change-me}
    volumes:
      - mongo-data:/data/db
    networks:
      - blindscloud-net

  rabbitmq:
    image: rabbitmq:3-management
    container_name: blindscloud-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-admin}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-change-me}
    ports:
      - "127.0.0.1:15672:15672"
    networks:
      - blindscloud-net

  auth-service:
    build:
      context: .
      dockerfile: services/auth-service/Dockerfile
    container_name: blindscloud-auth-service
    restart: unless-stopped
    environment:
      PORT: 4001
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
      SEED_DEMO: ${SEED_DEMO:-true}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  users-service:
    build:
      context: .
      dockerfile: services/users-service/Dockerfile
    container_name: blindscloud-users-service
    restart: unless-stopped
    environment:
      PORT: 4002
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  businesses-service:
    build:
      context: .
      dockerfile: services/businesses-service/Dockerfile
    container_name: blindscloud-businesses-service
    restart: unless-stopped
    environment:
      PORT: 4003
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  customers-service:
    build:
      context: .
      dockerfile: services/customers-service/Dockerfile
    container_name: blindscloud-customers-service
    restart: unless-stopped
    environment:
      PORT: 4004
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  jobs-service:
    build:
      context: .
      dockerfile: services/jobs-service/Dockerfile
    container_name: blindscloud-jobs-service
    restart: unless-stopped
    environment:
      PORT: 4005
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  products-service:
    build:
      context: .
      dockerfile: services/products-service/Dockerfile
    container_name: blindscloud-products-service
    restart: unless-stopped
    environment:
      PORT: 4006
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  pricing-service:
    build:
      context: .
      dockerfile: services/pricing-service/Dockerfile
    container_name: blindscloud-pricing-service
    restart: unless-stopped
    environment:
      PORT: 4007
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  billing-service:
    build:
      context: .
      dockerfile: services/billing-service/Dockerfile
    container_name: blindscloud-billing-service
    restart: unless-stopped
    environment:
      PORT: 4008
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  notifications-service:
    build:
      context: .
      dockerfile: services/notifications-service/Dockerfile
    container_name: blindscloud-notifications-service
    restart: unless-stopped
    environment:
      PORT: 4009
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  files-service:
    build:
      context: .
      dockerfile: services/files-service/Dockerfile
    container_name: blindscloud-files-service
    restart: unless-stopped
    environment:
      PORT: 4010
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
      UPLOAD_DIR: /data/uploads
      FILES_BASE_URL: ${FILES_BASE_URL:-}
    volumes:
      - uploads-data:/data/uploads
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  orders-service:
    build:
      context: .
      dockerfile: services/orders-service/Dockerfile
    container_name: blindscloud-orders-service
    restart: unless-stopped
    environment:
      PORT: 4011
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  demo-requests-service:
    build:
      context: .
      dockerfile: services/demo-requests-service/Dockerfile
    container_name: blindscloud-demo-requests-service
    restart: unless-stopped
    environment:
      PORT: 4012
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  module-permissions-service:
    build:
      context: .
      dockerfile: services/module-permissions-service/Dockerfile
    container_name: blindscloud-module-permissions-service
    restart: unless-stopped
    environment:
      PORT: 4013
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  models3d-service:
    build:
      context: .
      dockerfile: services/models3d-service/Dockerfile
    container_name: blindscloud-models3d-service
    restart: unless-stopped
    environment:
      PORT: 4014
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  model-permissions-service:
    build:
      context: .
      dockerfile: services/model-permissions-service/Dockerfile
    container_name: blindscloud-model-permissions-service
    restart: unless-stopped
    environment:
      PORT: 4015
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  activity-logs-service:
    build:
      context: .
      dockerfile: services/activity-logs-service/Dockerfile
    container_name: blindscloud-activity-logs-service
    restart: unless-stopped
    environment:
      PORT: 4016
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  user-sessions-service:
    build:
      context: .
      dockerfile: services/user-sessions-service/Dockerfile
    container_name: blindscloud-user-sessions-service
    restart: unless-stopped
    environment:
      PORT: 4017
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change}
      MONGO_URL: ${MONGO_URL:-mongodb://${MONGO_ROOT_USER:-admin}:${MONGO_ROOT_PASSWORD:-change-me}@mongo:27017/admin}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://${RABBITMQ_USER:-admin}:${RABBITMQ_PASSWORD:-change-me}@rabbitmq:5672}
      EVENT_EXCHANGE: ${EVENT_EXCHANGE:-blindscloud.events}
    depends_on:
      - mongo
      - rabbitmq
    networks:
      - blindscloud-net

  api-gateway:
    build:
      context: .
      dockerfile: services/api-gateway/Dockerfile
    container_name: blindscloud-api-gateway
    restart: unless-stopped
    environment:
      PORT: 3001
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173}
      AUTH_SERVICE_URL: http://auth-service:4001
      USERS_SERVICE_URL: http://users-service:4002
      BUSINESSES_SERVICE_URL: http://businesses-service:4003
      CUSTOMERS_SERVICE_URL: http://customers-service:4004
      JOBS_SERVICE_URL: http://jobs-service:4005
      PRODUCTS_SERVICE_URL: http://products-service:4006
      PRICING_SERVICE_URL: http://pricing-service:4007
      BILLING_SERVICE_URL: http://billing-service:4008
      NOTIFICATIONS_SERVICE_URL: http://notifications-service:4009
      FILES_SERVICE_URL: http://files-service:4010
      ORDERS_SERVICE_URL: http://orders-service:4011
      DEMO_REQUESTS_SERVICE_URL: http://demo-requests-service:4012
      MODULE_PERMISSIONS_SERVICE_URL: http://module-permissions-service:4013
      MODELS3D_SERVICE_URL: http://models3d-service:4014
      MODEL_PERMISSIONS_SERVICE_URL: http://model-permissions-service:4015
      ACTIVITY_LOGS_SERVICE_URL: http://activity-logs-service:4016
      USER_SESSIONS_SERVICE_URL: http://user-sessions-service:4017
    ports:
      - "3001:3001"
    depends_on:
      - auth-service
      - users-service
      - businesses-service
      - customers-service
      - jobs-service
      - products-service
      - pricing-service
      - billing-service
      - notifications-service
      - files-service
      - orders-service
      - demo-requests-service
      - module-permissions-service
      - models3d-service
      - model-permissions-service
      - activity-logs-service
      - user-sessions-service
    networks:
      - blindscloud-net

networks:
  blindscloud-net:
    driver: bridge

volumes:
  mongo-data:
  uploads-data:
